# Ignore OpenTofu's cache, binary, and lock files
.tofu
.tofu.lock.hcl
.terraform.lock.hcl

# Ignore sensitive files
*.tfvars

# Ignore the state file and its backups
terraform.tfstate
terraform.tfstate.backup
